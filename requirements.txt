# DeepAgents
deepagents>=0.1.0

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
starlette>=0.36.0

# Database Connectors
sqlalchemy>=2.0.0
alembic>=1.13.0  # 数据库迁移
pymysql>=1.1.0
psycopg2-binary>=2.9.0
clickhouse-driver>=0.2.0
oracledb>=1.3.0  # Oracle Database driver (thin mode, no Oracle client needed)
sqlite3  # 内置

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Caching
redis>=5.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
cryptography>=41.0.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Logging & Monitoring
loguru>=0.7.0
prometheus-client>=0.19.0  # 监控指标

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
pytest-cov>=4.1.0  # 测试覆盖率

# Development Tools
black>=23.0.0  # 代码格式化
isort>=5.12.0  # import 排序
mypy>=1.7.0    # 类型检查
flake8>=6.1.0  # 代码检查

# Utilities
python-dotenv>=1.0.0  # 环境变量
python-dateutil>=2.8.0  # 日期处理
pyyaml>=6.0  # YAML 解析
requests>=2.31.0  # HTTP 客户端
aiofiles>=23.0.0  # 异步文件操作

# 生产环境可选
# gunicorn>=21.0.0  # WSGI 服务器（如果需要）
# uvicorn-workers>=0.3.0  # 多进程支持
